rank_bm25
numpy
python-dotenv
tiktoken
tabulate
fastapi
uvicorn[standard]
python-multipart
httpx
groq
sentry-sdk[fastapi,celery]

# Gemini (Google AI) SDK — used for OCR / vision extraction in scanned PDFs
google-genai

passlib[bcrypt]
bcrypt==4.0.1   # 4.1.x removed __about__; passlib breaks without this pin
sqlalchemy[asyncio]
asyncpg               # Async Postgres driver (fast, works with FastAPI)
alembic               # Handles DB schema changes over time


redis                 # Python client for Redis


celery[redis]                  # Task queue; RabbitMQ broker + Redis result backend

qdrant-client>=1.16,<1.17  # Python client for Qdrant (match server version)

pypdf                 # Read PDFs, detect if scanned or digital
pdfplumber            # High-fidelity digital PDF extraction (preserves tables)

boto3                          # S3-compatible API (Cloudflare R2 object storage)
pydantic-ai           # Strictly-typed agent frameworks

cohere                # Semantic Reranking API

# Production hardening
slowapi               # In-process rate limiting for FastAPI
python-json-logger    # Structured JSON log formatting
python-jose[cryptography]  # JWT verification (Supabase auth)
supabase  # Supabase client (frontend auth)

# Pydantic email validation (EmailStr)
email-validator>=2.0

psycopg2-binary==2.9.9

# ── Master Orchestrator (LangGraph + Groq LLM) ──────────────────────────────
langgraph>=1.0.0              # State machine graph for agentic workflows
langgraph-checkpoint-postgres>=3.0.0  # Async Postgres checkpointer
langchain-core>=1.2.0         # Base abstractions (messages, LLMs)
langchain-groq>=1.1.0         # ChatGroq adapter for Llama 3.3 70B
psycopg[binary]>=3.1.0        # Async Postgres driver for LangGraph checkpointer

# ── Due Diligence pipeline (voyage-law-2, Neo4j, SPLADE) ────────────────────
voyageai                  # voyage-law-2 legal embedding model (primary)
neo4j>=5.20.0             # Neo4j AuraDB driver
sentence-transformers     # nomic-embed-text-v1.5 backup embedder
fastembed                 # SPLADE sparse vector embedder
python-docx               # DOCX document parsing
pymupdf                   # PDF parsing via fitz (legal document parser)

# ── Notification Service (Slack, Resend email, .ics calendar) ───────────────
icalendar                 # .ics calendar file generation for deadline notifications
python-dateutil            # Timezone utilities for .ics generation

# NLP pipeline
spacy>=3.7
